{
  "name": "arbitrage-public-legacy-sdk",
  "private": true,
  "description": "Archived SDK workspace. Dependencies are intentionally not installed here; use the Docker Compose root test entrypoint.",
  "scripts": {
    "test": "echo \"Archived SDK workspace: use docker compose run --rm test from the repo root.\" && exit 0"
  }
}
