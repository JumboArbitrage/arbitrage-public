{
  "name": "loal-version",
  "version": "1.0.0",
  "description": "Archived local TradeInit demo. Dependencies are intentionally not installed here.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Archived TradeInit demo: use docker compose run --rm test from the repo root.\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "private": true
}
